{
  "devDependencies": {
    "cypress": "^12.13.0",
    "cypress-mochawesome-reporter": "^3.5.0",
    "mochawesome": "^7.1.3",
    "mochawesome-report-generator": "^6.2.0"
  },
  "scripts": {
    "test": "cypress open",
    "test:report": "cypress run --spec cypress/e2e/testpage.cy.js && cypress-html-reporter --output reports --template basic"
    
  }
}
